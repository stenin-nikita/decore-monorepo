export const DEFAULT_FILE_NAME = 'manifest.client.json';

export const VITE_MANIFEST_PATH = '.vite/manifest.json';

export enum DevScripts {
  VITE_CLIENT = '@vite/client',
  REACT_REFRESH = '@vesta/react-refresh',
}
